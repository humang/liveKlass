apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: default
  labels:
    app: streamlit-dashboard
spec:
  type: ClusterIP
  selector:
    app: streamlit-dashboard
  ports:
    - name: http
      port: 80
      targetPort: 8501
